from telegram import Update from telegram.ext import ContextTypes from app.database import SessionLocal from app.states import State from app.services.session_service import load from app.services.admin_service import get_admin from app.customer.flow import ( customer_start, protect_start, handle_state, my_numbers, activate_start, account, support, help_customer, ) from app.admin.flow import ( admin_entry, dashboard_handler, payments, admin_state, section, admin_extended_state, ) from app.config import settings from app.keyboards import customer_main_menu CUSTOMER_BUTTONS = { '🛡️ حماية رقم': protect_start, '📱 أرقامي': my_numbers, '👤 حسابي': account, '💬 الدعم': support, '❓ المساعدة': help_customer, '🛡️ تفعيل الحماية': activate_start, '🏠 الرئيسية': customer_start, } ADMIN_BUTTONS = { '🏠 لوحة التحكم': dashboard_handler, '💳 المدفوعات': payments, '👥 العملاء': lambda u, c: section(u, c, 'customers'), '📱 الأرقام': lambda u, c: section(u, c, 'numbers'), '🛡️ الاشتراكات': lambda u, c: section(u, c, 'subscriptions'), '🔄 المتابعة': lambda u, c: section(u, c, 'followups'), '💬 الدعم': lambda u, c: section(u, c, 'support'), '🔔 الإشعارات': lambda u, c: section(u, c, 'notifications'), '📊 التقارير': lambda u, c: section(u, c, 'reports'), '❓ الأسئلة الشائعة': lambda u, c: section(u, c, 'faq'), '⚙️ الإعدادات': lambda u, c: section(u, c, 'settings'), '🧾 سجل العمليات': lambda u, c: section(u, c, 'audit'), } async def start(update, context): async with SessionLocal() as db: admin = await get_admin( db, update.effective_user.id, ) if admin or update.effective_user.id in settings.admin_ids: await admin_entry(update, context) else: await customer_start(update, context) async def message_router( update: Update, context: ContextTypes.DEFAULT_TYPE, ): if not update.message or not update.effective_user: return uid = update.effective_user.id text = (update.message.text or '').strip() # استعلام واحد للجلسة + استعلام واحد فقط للمدير. # get_admin() يعيد سجل المدير مباشرة بدل is_admin() ثم get_admin(). async with SessionLocal() as db: state, data = await load(db, uid) admin = await get_admin(db, uid) is_admin_user = bool(admin) or uid in settings.admin_ids # ========================= # ADMIN # ========================= if is_admin_user: if text in ADMIN_BUTTONS: return await ADMIN_BUTTONS[text]( update, context, ) if text.startswith(('✅ قبول ', '❌ رفض ')): return await admin_state( update, context, state or State.ADMIN_MENU, data, ) if state and str(state).startswith('admin_'): if state in ( State.ADMIN_SETTINGS_VALUE, State.ADMIN_SETTINGS_CONFIRM, State.ADMIN_TICKET_SELECT, State.ADMIN_TICKET_REPLY, ): return await admin_extended_state( update, context, state, data, ) return await admin_state( update, context, state, data, ) # ========================= # CUSTOMER # ========================= if ( text in CUSTOMER_BUTTONS and ( not state or state == State.IDLE ) ): return await CUSTOMER_BUTTONS[text]( update, context, ) # ========================= # ACTIVE STATE # ========================= if state and state != State.IDLE: return await handle_state( update, context, state, data, ) # ========================= # DEFAULT # ========================= await update.message.reply_text( 'اختر إحدى الخدمات من القائمة:', reply_markup=customer_main_menu(), ) 
